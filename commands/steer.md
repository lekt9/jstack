---
description: Re-aim the north star of an active Jesus Loop session
argument-hint: [--session NAME] "<new north star>"
---

Run this to redirect the loop without restarting it. The new north star is
re-injected at every step firing thereafter.

!`bash ${CLAUDE_PLUGIN_ROOT}/scripts/steer.sh $ARGUMENTS`
